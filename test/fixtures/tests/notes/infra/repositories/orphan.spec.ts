// Tests a NoteCache that has since been deleted. No source file claims this
// spec, so the reverse check must list it as unmatched rather than silently
// dropping it.
describe('removed NoteCache', () => {
  it('is a placeholder', () => {});
});
